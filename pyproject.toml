[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]  # erreurs pycodestyle, pyflakes, imports, upgrades, bugbear

[tool.black]
line-length = 100
target-version = ["py311"]